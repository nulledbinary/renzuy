# Local toolchain — re-installed inside the image
tools/

# Build outputs / caches
.gradle/
build/
app/build/
app/bin/
youtube-source/build/
youtube-source/bin/

# IDE / editor
.vscode/
.idea/
*.iml

# Git
.git/
.gitignore
.gitattributes

# Secrets — production secrets come from ECS task definition env vars
.env
.env.*
!.env.example

# CI / docs / screenshots — not needed in the image
.github/
*.md
*.png

# Local-only deploy artifacts (task defs, IAM policies) — not consumed by Docker.
# entrypoint.sh lives at the repo root and is git-tracked so it isn't affected.
deploy/

# OS junk
Thumbs.db
.DS_Store
